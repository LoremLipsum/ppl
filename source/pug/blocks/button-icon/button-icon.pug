mixin button-icon(data)
    if data.type == 'submit'
        +el({
            tag: data.tag ? data.tag : 'label',
            class: 'button-icon',
            mod: data.mod,
            mix: data.mix,
            js: data.js,
            ariaLabel: 'Искать',
        })
            if data.icon
                +el({
                    tag: 'svg',
                    icon: data.icon,
                    class: 'button-icon__icon',
                })
            +el({
                tag: 'input',
                class: 'hidden',
                type: 'submit',
                disabled: data.disabled,
            })&attributes(attributes)

    else
        +el({
            tag: data.tag ? data.tag : 'button',
            class: 'button-icon',
            mix: data.mix,
            mod: data.mod,
            js: data.js,
            before: data.before,
            after: data.after,
            position: data.position,
            title: data.title,
            target: data.target,
            href: data.href
        })&attributes(attributes)
            if data.icon
                +el({
                    tag: 'svg',
                    icon: data.icon,
                    class: 'button-icon__icon',
                })
            if  data.badge
                +el({
                    tag: 'span',
                    class: 'button-icon__badge',
                    text: data.badge
                })
            if data.close
                +el({
                    tag: 'span',
                    class: 'button-icon__close',
                })
